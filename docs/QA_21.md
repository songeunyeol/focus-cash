# FocusCash QA — 21차 (2026-09-09)

브랜치: eat/19b-server-migration-cont @ 6c2ecbf (이후 이 문서 커밋 포함)  
워크트리: .claude/worktrees/feat19-cont  
기준: lutter analyze 0이슈 + 코드/설정 스캔 (+ 심층 스캔 보강)

다음에 /focus-cash 또는 이 문서부터 이어서 처리하면 된다. **PR 불필요** — 피처 브랜치에 커밋만.

---

## P0 막힘 / 출시 위험

1. **서버 스위치 OFF + rules v1**  
   - evidence: lib/config/app_config.dart — useServerSettlement=false, useServerStore=false  
   - irestore.rules (v1) vs irestore.rules.next (배포 전)  
   - impact: 클라 잔액 쓰기·기프티콘 광역 read 등 문서상 S1–S6 구멍 유지  
   - next: docs/SERVER_MIGRATION.md 0~4 — Functions → 스위치 ON → rules v2

2. **카카오 로그인 = Functions 필수**  
   - evidence: lib/services/auth_service.dart (구 이메일/비번 경로 제거, kakaoSignIn만)  
   - impact: Functions 미배포 시 카카오 전부 실패. 구글은 가능  
   - next: Functions 배포 후 카카오 E2E

3. **텔레그램 봇 토큰이 클라에 잔존**  
   - evidence: lib/config/secrets.dart, 	elegram_service.dart, store_service.dart (useServerStore=false면 클라 발송)  
   - impact: APK에 토큰 포함 가능  
   - next: 토큰 회전 + 서버 이전 후 클라 경로 삭제

4. **pp_config/android 권한**  
   - evidence: 에뮬 PERMISSION_DENIED; checkMinimumVersion=true  
   - impact: 강제 업데이트 사실상 미가동(실패 시 통과)  
   - next: rules v2 + 콘솔 문서 생성

---

## P1 버그 · 불일치

1. **날짜 키 KST 부분 적용**  
   - 월간/focusDate: KST (ocus_day.dart / ocusDateKey)  
   - 캘린더 dateKeyOf: 로컬 (
ecords_shared.dart)  
   - 주간 _periodStart / getWeeklyStats: 로컬 weekday (ocus_service.dart)  
   - 크레딧 creditDate: 로컬 (credit_service.dart)  
   - impact: TZ≠KST 또는 자정 전후 불일치  
   - next: ocusDateKey로 통일

2. **users.todayFocusMinutes stale 쓰기**  
   - UI는 effectiveTodayFocusMinutes()로 보정  
   - Firestore raw 필드는 다음 완료 전까지 어제 값 잔존  
   - next: (선택) 자정 리셋 쓰기 또는 서버 집계

3. **포기 세션은 주간/캘린더/랭킹 집계 제외** (의도적, UX 인지 필요)

4. **몰입 밝기 저하 = Android만**  
   - evidence: screen_brightness_service.dart non-Android early return  
   - next: iOS 구현 또는 문구에 Android-only 명시

5. **AppTheme 정적 색 = 다크 고정**  
   - 라이트 모드에서도 다크 팔레트 참조 (	heme.dart + profile/friends/gifticons/onboarding)  
   - next: context.ds 이행

6. **에뮬 GMS DEVELOPER_ERROR** — 광고/일부 GMS, 실기기 SHA 확인

---

## P2 미구현 · 스켈레톤

1. 디자인 v2 잔여 AppTheme: profile / friends / my_gifticons / onboarding / level_frame  
2. ssets/images/mascot_expressions.png — 코드 미연결  
3. 서버 이관 후 클라 삭제 예정 코드 아직 유지 (스위치 ON 전엔 정상)  
4. irebase_options.dart TODO 주석 잔재

---

## P3 죽은 코드 / 잔재

1. **고아 위젯** (import 없음): TodayStatsCard, QuickStartButton  
2. 구 라우트 /achievements, /focus-calendar → Records 탭 리다이렉트 (호환용, 죽지 않음)  
3. Friends / MyGifticons / NotificationSettings — named route 없음, MaterialPageRoute로만 진입 (orphan 아님)  
4. 스플래시(main.dart) — 타이머+AppTheme, 브랜드 아이콘/마스코트와 불일치  
5. 	heme.dart 파사드 glowButton / primaryGradient 잔재

---

## 정상 확인

- 주간 0분 픽스: effectiveTodayFocusMinutes + 포기 시 집계 제외 (6206a5)  
- 브랜드: 런처 아이콘, DsMascotSlot←mascot_front, 몰입←mascot_immersion (6c2ecbf)  
- 몰입: 15초 idle, Android 밝기, 설정 토글  
- 기록 4탭, BottomNav 홈/상점/랭킹/프로필 일치  
- secrets / google-services / jks gitignore

---

## 추천 다음 一手 (우선순위)

1. (사용자) Functions · 시크릿 · rules v2 — SERVER_MIGRATION.md  
2. 날짜 키 KST 통일 (캘린더·주간·creditDate)  
3. 고아 위젯 삭제 + 스플래시 브랜드 교체  
4. friends / gifticons / profile AppTheme → DS  
5. (선택) mascot_expressions 연동  
6. feat/19b 실기기 검증 후 main 머지 (그때 PR)

---

## 관련 커밋

- 6206a5 fix: todayFocusMinutes / 포기 집계  
- 6c2ecbf feat: brand icon and mascot (session 21)  
