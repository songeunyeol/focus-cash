# Focus Cash - 프로젝트 진행상황

마지막 업데이트: 2026-09-09 (19차 세션 — 전체 리뷰 → 보안/정산 레이어 착수)

---

## 전체 진행률


```
Phase 1 (핵심 기능)      ████████████████████  100%
Phase 2 (게이미피케이션)  ████████████████████  100%
Phase 3 (소셜/랭킹)      █████████████████░░░   85%
Firebase 연동            ████████████████████  100%
관리자 앱                ████████████████████  100%
실기기 테스트            ████████████████████  100%  ← 에뮬 + 실기기 완료
서버/보안 레이어         ████████░░░░░░░░░░░░   40%  ← 19차 신규: Functions 8개 작성·컴파일 완료, 배포 전
테스트 안전망            ████████░░░░░░░░░░░░   40%  ← 도메인 단위 테스트 43개 + 서버 6개
앱스토어 출시            ████████░░░░░░░░░░░░   40%  ← 보안 이슈로 재산정. Functions 배포 + 규칙 v2 전엔 출시 불가
디자인 리뉴얼 v1         ████████████████████  100%  ← 인디고/시안 테마 (18차에 폐기)
디자인 리뉴얼 v2         ██████░░░░░░░░░░░░░░   30%  ← 토큰+홈 완료 / 나머지 16화면 남음

전체 진행률: 약 80% (19차 리뷰로 재산정 — "실물 지급 앱의 서버 검증" 이 빠져 있었음)
```

---

## 19차 세션 (2026-09-09) — 전체 리뷰 → 보안/정산 레이어 + 버그 일괄

Fable 5.1 로 기획·코드·디자인 전체 리뷰를 먼저 했다. 결론: **기능·디자인 시스템은 좋은데,
크레딧이 실물로 바뀌는 앱에 서버 검증이 없다.** 배포된 Firestore 규칙 기준으로 본인 ID 토큰만 있으면
크레딧 자가 발급·미사용 기프티콘 코드 열람·익명 로그인으로 관리자 권한 획득이 가능했다.
디자인 이행보다 이걸 먼저 하기로 하고 착수했다. 작업 브랜치: `feat/19-security-bugs-domain` (main 미머지).

### 도메인 로직 추출 + 테스트 (TDD)
| 항목 | 파일 |
|------|------|
| 크레딧·일일한도·페널티 규칙 | `lib/domain/credit_rules.dart` |
| 스트릭 판정 (lastFocusDate 기준) | `lib/domain/streak_rules.dart` |
| 배지·XP 판정 (before 값 입력) | `lib/domain/badge_rules.dart` |
| 가중 추첨 + 기댓값 | `lib/domain/weighted_pick.dart` |
| 강제종료 복구 판정 + 세션 직렬화 | `lib/domain/session_recovery.dart` |
| 단위 테스트 43개 | `test/domain/*_test.dart` |
| 서비스 Firestore 지연 주입 (테스트 가능) | credit/focus/auth_service |

### 정산 구조 변경
| 항목 | 내용 |
|------|------|
| **통계·스트릭·XP·배지를 한 트랜잭션으로** | `FocusService.endSession` → `(FocusSession, SessionOutcome)`. 기존엔 XP 단계가 이미 갱신된 값을 before 로 읽어 `first_focus` 가 절대 안 나오고 `streak_7` 이 6일차에 나왔다 |
| 스트릭 기준 `lastActiveAt` → `lastFocusDate` | 룰렛만 돌린 날이 연속 인정 / 룰렛 후 완료 시 스트릭 안 오르던 버그 |
| 일일 적립 한도 250 강제 | 약관 제4조 ② 와 일치. 집중·광고 보너스에만 적용. 설정 화면에 잔여 한도 안내 |
| 가입 보너스 → 첫 집중 완료 보너스 | 탈퇴→재가입 무한 파밍 차단. `firstFocusBonusGiven` 플래그 |
| 룰렛 폴백 EV 116.5 → 24.05 | Firestore 설정 없을 때 운영자 손해였음 |
| 룰렛 순서: 잔액 확인 → 횟수 소모, 초과 시 환불 | 크레딧 부족이면 횟수만 날아가던 것 |

### 버그 수정 (리뷰에서 확정한 11건)
| # | 내용 | 파일 |
|---|------|------|
| B1 | 스트릭 기준 필드 (위) | focus_service |
| B2 | `cleanupOrphanedSessions` 가 `completed isNull` 로 영원히 매치 안 됨 → `endedAt isNull` | focus_service |
| B3 | 가입 아바타 목록(📚🎯…)이 전역(🦁🐯…)과 달라 다른 아바타 표시 | signup_profile_screen |
| B4 | `getWeeklyStats` 미완료 세션 합산 → completed 필터 | focus_service |
| B5 | 상태바 아이콘 밝기 light 고정 → 테마 추종 (`DsTheme.overlayStyleFor`) | main, ds_theme, home |
| B6 | 탈퇴 시 배송지·메모·응모 미삭제 + Auth 삭제 순서 + requires-recent-login 처리 | auth_service, profile_screen |
| B7 | 시간대 약어(KST) 조회 실패로 항상 서울 폴백 → `flutter_timezone` | notification_service |
| B8 | 시작 광고 로드 실패 시 무안내 → 스낵바 | focus_screen |
| B9 | 룰렛 등급색 6종 하드코딩 → 무채색→불꽃 단계 | store_screen |
| B10 | 친구 토스트 배경 구 인디고 잔재 → 테마 | friends_screen |
| B11 | `showFocusCompleted` 호출처 없음 → 백그라운드 완료 시 알림 / `onInviteSuccess` 호출처 없음 → invite_3 배지 연결 | focus_provider |

### 신규 기능
| 항목 | 파일 |
|------|------|
| **진행 중 세션 로컬 영속화 + 강제종료 복구** (이어하기/완료 처리/폐기 다이얼로그) | focus_provider, home_screen |
| **집중 화면 강제 다크** (OLED 배터리 — 18차 미결 1번 답) | routes.dart `Theme(data: DsTheme.dark())` |
| Analytics 이벤트 7종 (start/complete/abandon/recovered/exchange/spin/raffle) | `services/analytics_service.dart` |
| 알림 설정 화면 (스트릭 리마인더·마케팅 수신 철회) — 약관이 참조하던 화면 | `profile/notification_settings_screen.dart` |
| 가입 시 만 14세 확인 체크 | signup_terms_screen |
| 미사용 의존성 제거 firebase_messaging / firebase_storage / intl | pubspec |

### 서버 (Cloud Functions, asia-northeast3) — 작성·컴파일·테스트 완료, **배포는 아직**
| 함수 | 역할 |
|------|------|
| `settleSession` | 서버 시각으로 경과 검증 → 크레딧·XP·스트릭·배지·첫집중 보너스 한 트랜잭션, 세션 id 멱등 |
| `grantAdBonus` | 시작/종료 광고 보너스, 세션당 1회, 일일 한도 |
| `redeemGifticon` | 잔액+차감+코드 발급 한 트랜잭션 (경합 시 후보 재시도) |
| `spinRoulette` | 서버 난수, 일일 한도, 재고 확인 |
| `enterRaffle` | 차감+티켓+마감 시 서버 추첨 |
| `kakaoSignIn` | 카카오 토큰 검증 → custom token (기존 이메일 계정 uid 보존) |
| `onUserDeleted` | 개인정보 정리 |
| `onDeliverySubmitted` | 텔레그램 알림 (토큰은 Secret Manager) |
- 공통 규칙 `functions/src/rules.ts` = `lib/domain/*` 미러, 테스트 6개
- `firestore.rules.next` = 규칙 v2 (경제 필드 클라 write 금지, 기프티콘 read 본인만, admin claim)
- `lib/config/app_config.dart` 스위치 3개 (전부 false) + `lib/services/server_api.dart` 래퍼. FocusProvider 는 스위치 켜면 서버 정산으로 감
- `functions/src/scripts/set-admin.ts` — 관리자 claim 부여
- 관리자 앱 로그인: PIN+익명 → **Google + admin claim** (`focus_cash_admin/lib/screens/login_screen.dart`)
- 키스토어 비번 `build.gradle.kts` 평문 → `android/key.properties`(gitignore)
- **전체 순서·롤백·체크리스트: `docs/SERVER_MIGRATION.md`**

### 검증
- `flutter analyze` 0건 (메인·관리자 앱) / `flutter test` 43개 통과 / `functions npm test` 6개 통과
- `flutter build apk --profile --target-platform android-x64` 성공 (56.7MB) — gradle key.properties·신규 패키지 정상

#### 다음 시작점 (순서 중요)
1. **`docs/SERVER_MIGRATION.md` 0~2단계** — 콘솔에서 현재 배포 규칙 백업 → 시크릿 회전(카카오 키·텔레그램 토큰) → Blaze → `firebase deploy --only functions` → set-admin
2. 3단계 — `AppConfig.useServerSettlement = true`, 내부 테스트 1회 완주로 `credit_transactions.source == 'server'` 확인
3. 4단계 — 규칙 v2 배포 (구버전 앱 강제 업데이트 안내 필요)
4. 실기기 검증: 강제종료 복구 다이얼로그 / 집중 화면 다크 / 알림 설정 / 상태바 / 만14세 체크 → main 머지
5. 그 다음에야 디자인 이행 계속 (기록 화면 통합 → focus_screen → store)

---

## 18차 세션 (2026-09-03) — 디자인 전면 리뉴얼 착수 (Phase 0~3)

앱이 "바이브코딩한 것"으로 읽히는 원인이 색이 아니라 **구조**에 있다고 진단.
인디고(#4F46E5)+시안 그라디언트에 glow 를 얹은 조합은 AI 생성 UI 의 클리셰였고,
레이아웃·위계·컴포넌트 형태는 토큰이 아니라 17개 화면 파일에 인라인으로 박혀 있었다
(BoxDecoration 139개, TextStyle 296개, radius 10종, fontSize 21종).

### 방향 확정
| 항목 | 결정 |
|------|------|
| 컨셉 | **계기(Instrument) + 보상(Reward) 이중 모드** — 집중은 조용하게, 보상만 화려하게 |
| 팔레트 | 전면 재설계. 인디고/시안/골드 제거, **단일 웜 액센트 불꽃 `#FF7A2F`** |
| 폰트 | **Pretendard 번들** (SIL OFL, 400/500/600/700 OTF 6.1MB) |
| 몰입 모드 | 집중 중 저전력 암전 화면 (풀버전, 미구현) |
| 앱 아이콘 | 웜 계열 재제작 — 코드 생성 초안 완료, 앱 미적용 |
| 마스코트 | 링 가운데가 아니라 **빈 상태(0분)** 자리로 확정 |

### Phase 0 — 폰트
| 항목 | 상태 |
|------|------|
| Pretendard static 4 weight + OFL 라이선스 번들 | ✅ `assets/fonts/` |
| pubspec fonts 섹션 + 이모지 폴백(Noto Color Emoji) | ✅ 완료 |

### Phase 1 — 로직 버그 4건 (디자인과 독립)
| 항목 | 상태/파일 |
|------|----------|
| **화면 꺼짐 = 세션 소실** — 안드로이드는 화면 꺼짐도 `paused` 로 준다 | ✅ `MainActivity.kt` + `screen_state_service.dart` |
| 백그라운드 Timer 스로틀링 → 복귀 시점 벽시계 판정으로 전환 | ✅ `focus_screen.dart` |
| 경과 시간 드리프트 (`_elapsedSeconds++` 누산, 보정 코드 부재) | ✅ `focus_provider.dart` |
| 벽시계 전환이 연 **기기 시각 조작 어뷰징** → 90초 점프 감지 시 무효 | ✅ `focus_provider.dart` |

### Phase 2 — 토큰 레이어 + 파사드
| 항목 | 상태 |
|------|------|
| `lib/design/` 8파일 신규 (색/타이포/간격/깊이/등급/모드/테마) | ✅ 완료 |
| `lib/config/theme.dart` 를 파사드로 축소 (공개 이름 유지) | ✅ 완료 |
| **화면 코드 0줄 수정으로 658개 호출부가 새 토큰 수신** | ✅ 완료 |
| `primaryGradient` 동일색 2스톱 트릭 → 호출부 28곳 안 건드리고 그라디언트 소멸 | ✅ 완료 |
| `level_frame.dart` 하드코딩 42색 임시 스왑 (형태 재설계는 추후) | ✅ 완료 |

### Phase 3 — 홈 화면 리디자인
| 항목 | 상태/파일 |
|------|----------|
| 홈의 임무를 하나로 축소 — "지금 집중할래?" | ✅ `home_screen.dart` 재작성 |
| `DsDial` — 눈금 60개가 채워지는 계기 (굵은 원호 ✗) | ✅ 신규 |
| `DsMascotSlot` — 0분일 때만 등장. 시안 나오면 Image.asset 교체 | ✅ 신규 |
| `DsButton` / `DsPressable` — 누름은 색이 아니라 불투명도, radius 6 | ✅ 신규 |
| 카드·그라디언트·glow·주간차트·최근기록 제거 | ✅ 완료 |
| `AppConstants.dailyGoalMinutes = 240` 신규 | ✅ 완료 |

**실기기에서만 잡힌 것 3건** — `18:00` 이 오후 6시로 읽힘(→ `18시간`) /
`StrokeCap.round` 가 눈금을 굵은 대시로 만듦(→ `butt`) /
한글 라벨에 라틴 대문자용 자간 1.6 적용한 실수(→ 제거)

### 작업 방식 메모
- 에뮬레이터 확인은 **`--profile` 빌드**로 할 것. `build.gradle` 에 profile 서명 설정이 없어
  디버그 키로 서명되므로 Google 로그인이 동작한다. 릴리즈 APK 는 SHA-1 미등록이라 로그인 실패.
- 에뮬레이터 `/data` 가 94% 차 있어 디버그 APK(117MB) 설치 불가. profile 은 46MB.
- 홈 리디자인 목업: https://claude.ai/code/artifact/9ad29a7b-64ca-48e9-bb36-752e844edb08

#### 다음 시작점
1. **라이트 모드에서도 배터리를 아낄 방안** ← 사용자 요청.
   OLED 에서 흰 배경은 전력 소모가 가장 크므로 "집중 중엔 강제 다크" 같은 장치가 필요한지 검토
2. **기록 화면** — 통계·캘린더·크레딧내역·업적 4개 화면을 탭으로 흡수 (화면 수가 줄어드는 유일한 단계)
3. 몰입 모드 구현 (Phase 4)
4. Phase 1 로직 수정의 **실기기 검증 7종 시나리오** — 에뮬레이터로는 판단 불가

---

## 17차 세션 (2026-04-12) — 앱 평가 + 캐릭터 마스코트 기획

### 기획 및 방향 결정
| 항목 | 상태 |
|------|------|
| 실기기 테스트 완료 확인 (에뮬 + 실기기) | ✅ 완료 |
| 앱 냉정 평가 (강점/약점 분석) | ✅ 완료 |
| 캐릭터 마스코트 방향 결정 (불꽃 "플로우") | ✅ 완료 |
| 화면 전환 애니메이션 계획 수립 | ✅ 완료 |
| 캐릭터 제작 도구 확정 (ChatGPT/Midjourney) | ✅ 완료 |

#### 다음 시작점
- ChatGPT/Midjourney로 불꽃 캐릭터 "플로우" 시안 제작
- 시안 확정 후 Flutter 앱 배치 구현
- `animations` 패키지 도입, 화면 전환 개선

---

## 16차 세션 (2026-04-09) — 버그/폴리싱 집중

### 완료
| 항목 | 파일 |
|------|------|
| 라이트 모드 교환창 버그 수정 (하드코딩 다크 배경) | `store_screen.dart` |
| analyze 경고 0개 (미사용 import/상수/중괄호 제거) | 다수 |
| 상점 탭 순서 변경: 응모방→룰렛→교환 | `store_screen.dart` |
| 레벨 프레임 애니메이션 업그레이드 (5단계 차별화) | `level_frame.dart` |
| BOTTOM OVERFLOW 수정 (_DayDetailSheet) | `focus_calendar_screen.dart` |
| BOTTOM OVERFLOW 수정 (_DeliveryFormSheet) | `my_gifticons_screen.dart` |
| 라이트 모드 하드코딩 색상 전수 점검 및 수정 | `my_gifticons_screen.dart` |

### 룰렛 추천 설정 (어드민 앱에서 적용)
| 상품 | 확률 |
|---|---|
| 5크레딧 | 55% |
| 10크레딧 | 25% |
| 30크레딧 | 10% |
| 80크레딧 | 6% |
| 200크레딧 | 3% |
| 500크레딧 | 1% |
기댓값 24 / 비용 50 / 하우스엣지 52%

#### 다음 시작점
- 실기기 테스트 (레벨 프레임 애니메이션, 테마 전환, 탭 순서 확인)
- Play Store 테스터 추가 + 앱 콘텐츠 설문 완료

---

## 프로젝트 개요

### 앱 소개
**Focus Cash** — 집중 타이머 앱. 집중할수록 크레딧을 쌓고, 크레딧으로 기프티콘/룰렛/응모를 할 수 있는 보상형 집중 앱.

### 핵심 철학
- 집중 방해 요소 제거 (일시정지 없음, 확인 팝업 없음)
- 하드코어 모드: 포기하면 크레딧 10% 차감 → 진짜 집중 유도
- 크레딧으로 실제 상품 교환 가능

### 프로젝트 구조
```
C:\Users\song\Desktop\anti claude\
├── focus_cash\          ← 메인 앱 (Flutter)
└── focus_cash_admin\    ← 관리자 앱 (Flutter)
```

**Firebase 프로젝트:** `focuscash-2676f`
- Firestore: 서울(asia-northeast3)
- 복합 인덱스: `firestore.indexes.json` 참고

---

## 환경 정보

| 항목 | 내용 |
|------|------|
| Flutter | 3.41.4 (stable) — `C:\src\flutter` |
| Flutter 실행 경로 | `C:\src\flutter\bin\flutter.bat` (PATH 미등록, 전체경로 사용) |
| FlutterFire CLI | 설치됨, PATH: `C:\Users\song\AppData\Local\Pub\Cache\bin` |
| Firebase CLI | `node "C:/Users/song/AppData/Roaming/npm/node_modules/firebase-tools/lib/bin/firebase.js"` |
| Android Studio | 설치됨 |

---

## 크레딧 경제 (`lib/config/constants.dart`)

| 항목 | 수치 |
|------|------|
| 집중 크레딧 | 10분당 10크레딧 (분당 1크레딧) |
| 세션당 최대 집중 | 120분 (2시간) |
| **일일 적립 한도** | **250 크레딧** (집중+광고 보너스, 약관 제4조 ②) — 19차부터 코드로 강제 |
| 하드코어 완료 보너스 | 기본 크레딧 × 1.2배 |
| 시작 광고 보너스 | +15 크레딧 |
| 종료 리워드 광고 보너스 | 기본 크레딧 × 20% (endAdMultiplierRate) |
| 룰렛 1회 | 50 크레딧 (rouletteCost) |
| 룰렛 일일 한도 | 3회 (rouletteDailyLimit) |
| 룰렛 폴백 기댓값 | 24.05 (하우스엣지 52%) |
| 아메리카노 교환 | 6,500 크레딧 |
| **첫 집중 완료 보너스** | 200 크레딧 (구 가입 보너스 — 19차에 이동) |
| 초대 보너스 | 200 + 200 (피초대자 첫 완료 시) |

---

## 주요 파일 구조

```
lib/
├── design/                     ⭐ 18차 신규 — 디자인 시스템
│   ├── ds.dart                 배럴 + context.ds / context.dsMode
│   ├── ds_colors.dart          ThemeExtension. 액센트는 불꽃 하나뿐
│   ├── ds_typography.dart      10역할 타입 스케일 + tabularFigures
│   ├── ds_dimens.dart          Sp(7단계) / R(4단계) / Motion / Stroke
│   ├── ds_surfaces.dart        깊이 e0~e3 (다크=명도+헤어라인, 라이트=그림자)
│   ├── ds_rarity.dart          등급 — 색이 아니라 형태로 구분
│   ├── ds_mode.dart            계기/보상/몰입 3모드 전파
│   └── ds_theme.dart           ThemeData 조립
├── config/
│   ├── constants.dart          크레딧 경제 + dailyGoalMinutes(18차)
│   ├── secrets.dart            텔레그램 봇 토큰 + 카카오 앱 키 (AppSecrets)
│   ├── theme.dart              ⚠️ 구 API 파사드 (화면 이행 완료 후 삭제 예정)
│   └── routes.dart             라우트 정의
├── models/
│   ├── user_model.dart         rouletteDate, rouletteSpinsToday 포함
│   ├── focus_session.dart
│   ├── store_item.dart
│   ├── roulette_config.dart    RoulettePrize.imageBase64 포함
│   ├── raffle_room.dart        closedAt 포함
│   ├── gifticon_code.dart      prizeType: 'gifticon'|'direct'|'roulette'
│   └── credit_transaction.dart
├── providers/
│   ├── auth_provider.dart      데모 코드 제거됨 (7차)
│   └── focus_provider.dart     addEndAdBonus() multiplier 방식 (× 20%)
├── services/
│   ├── auth_service.dart
│   ├── focus_service.dart      getRanking(), cleanupOrphanedSessions() 포함
│   ├── credit_service.dart     addCredits() todayCredits 업데이트 포함 (7차)
│   ├── store_service.dart      getAvailableCounts(), saveRouletteWinRecord() 포함
│   ├── friend_service.dart     watchSentPendingUids(), cancelFriendRequest() 포함
│   ├── screen_state_service.dart  ⭐ 18차 — 화면 꺼짐/앱 이탈 구분 (MethodChannel)
│   ├── notification_service.dart
│   ├── ad_service.dart         ← 실제 AdMob ID 교체 필요
│   └── telegram_service.dart   AppSecrets 참조
├── widgets/
│   ├── common/                 ⭐ 18차 신규 — 공통 컴포넌트
│   │   ├── common.dart         배럴
│   │   ├── ds_dial.dart        눈금 60개 계기 다이얼 (CustomPainter)
│   │   ├── ds_button.dart      primary/quiet/ghost, 보상 모드면 자동 pill
│   │   ├── ds_pressable.dart   누름 = 불투명도 전환
│   │   └── ds_mascot_slot.dart 마스코트 자리 (0분일 때만 등장)
│   ├── level_frame.dart        ⚠️ 임시 색 스왑만 — 형태 기반 재작성 예정
│   ├── today_stats_card.dart   ⚠️ 홈에서 제거됨 (기록 화면으로 이전 예정)
│   ├── quick_start_button.dart ⚠️ 홈에서 제거됨
│   ├── weekly_chart.dart       focus_stats_screen 에서만 사용
│   └── credit_display.dart     ⚠️ 홈에서 제거됨
└── screens/
    ├── auth/
    │   ├── login_screen.dart         구글/카카오 로그인만 (데모 버튼 제거됨)
    │   └── signup_profile_screen.dart
    ├── home/home_screen.dart         18차 재작성 — 다이얼 하나 + 시작 버튼 하나
    ├── focus/
    │   ├── focus_setup_screen.dart   하드코어 예상크레딧 1.2배 반영 (7차)
    │   └── focus_screen.dart         18차 — 화면꺼짐/이탈 구분 라이프사이클
    ├── store/store_screen.dart       교환/룰렛(확률%표시)/응모방 탭
    ├── ranking/ranking_screen.dart   전체/친구 랭킹 (일간/주간/월간)
    ├── social/friends_screen.dart    친구 탭3개(친구·요청·찾기), 토글요청
    └── profile/
        ├── profile_screen.dart
        ├── my_gifticons_screen.dart  4종 카드 (_Gifticon/_DirectDelivery/_Roulette/_Raffle)
        ├── credit_history_screen.dart Firestore 실데이터 연동 (7차)
        └── focus_stats_screen.dart
```

### 관리자 앱 주요 구조
```
focus_cash_admin/lib/
├── services/admin_service.dart     Firestore CRUD
└── screens/
    ├── login_screen.dart           PIN: 6170, 익명 로그인
    ├── dashboard_screen.dart
    ├── store/
    │   ├── store_items_screen.dart
    │   └── gifticon_codes_screen.dart
    ├── roulette/roulette_screen.dart
    └── raffle/raffle_screen.dart
```

---

## Firestore 컬렉션 구조

| 컬렉션 | 용도 | 관리 주체 |
|--------|------|----------|
| `users` | 유저 정보, 크레딧, 스트릭, 룰렛 스핀 카운트 | 앱 자동 |
| `focus_sessions` | 집중 세션 기록 | 앱 자동 |
| `credit_transactions` | 크레딧 내역 | 앱 자동 |
| `friend_requests` | 친구 요청 (status: pending/accepted/rejected) | 앱 자동 |
| `store_items` | 교환 상품 목록 | 관리자 앱 |
| `roulette_config` | 룰렛 설정 (doc: `config`) | 관리자 앱 |
| `raffle_rooms` | 응모방 | 관리자 앱 |
| `raffle_entries` | 응모 내역 (docId: `{userId}_{roomId}`) | 앱 자동 |
| `gifticon_codes` | 기프티콘 코드 | 혼합 |

### gifticon_codes prizeType 규칙
| storeItemId | 생성 경로 | prizeType |
|-------------|----------|-----------|
| `{uuid}` | 상점 직접 교환 | `'gifticon'` |
| `roulette_win` | 룰렛 크레딧 당첨 기록 | `'roulette'` |
| `raffle_{roomId}` | 응모방 기프티콘 당첨 | `'gifticon'` |
| `raffle_{roomId}` | 응모방 직접배송 당첨 | `'direct'` |

### Firestore 보안 규칙 현황 (`firestore.rules`)
- `users`: 인증 유저 읽기, 본인만 쓰기
- `focus_sessions`: 인증 유저 읽기, 본인 세션만 쓰기
- `credit_transactions`: 본인 것만 읽기/쓰기
- `friend_requests`: 발신/수신자만 읽기/쓰기
- `gifticon_codes`: 인증 유저 전체 읽기/쓰기 (count() 쿼리 때문)
- `store_items`, `raffle_rooms`, `roulette_config`: 인증 유저 전체 읽기/쓰기 (관리자 앱)
- `raffle_entries`: 인증 유저 읽기, 본인만 생성

복합 인덱스: `firestore.indexes.json` — friend_requests 4개 포함, 배포 완료

---

## 세션별 작업 기록

### 15차 세션 (2026-03-31) — GitHub 연동 + 라이트/다크 테마 전환 + v1.0.0+3 빌드

#### GitHub 연동
| 항목 | 상태 |
|------|------|
| `songeunyeol/focus-cash` public 레포 연결 | ✅ 완료 |
| `.gitignore` 보안 처리 (secrets.dart / google-services.json / *.jks) | ✅ 완료 |
| `secrets.dart.example` 템플릿 생성 | ✅ 완료 |
| 초기 커밋 (161개 파일) + push | ✅ 완료 |
| `/focus-cash-save` 스킬 GitHub push 단계 추가 | ✅ 완료 |

#### 버그 수정
| 항목 | 상태 |
|------|------|
| 친구 랭킹 불러오기 실패 — Firestore 인덱스 미배포 → 재배포 | ✅ 해결 |

#### 라이트/다크 테마 전환
| 항목 | 파일 |
|------|------|
| ThemeProvider + SharedPreferences 영구 저장 | theme_provider.dart (신규) |
| `_AppColors` 클래스 + `AppTheme.of(context)` + `lightTheme` | theme.dart |
| main.dart ThemeProvider + Consumer 적용 | main.dart |
| 프로필 화면 다크 모드 토글 스위치 | profile_screen.dart |
| 17개 화면/위젯 파일 전체 색상 교체 | 전체 screens/, widgets/ |

#### 빌드
| 항목 | 상태 |
|------|------|
| 버전 1.0.0+2 → 1.0.0+3 | ✅ 완료 |
| app-release.aab 빌드 (51.9MB) | ✅ 완료 |
| Play Store 내부 테스트 aab 업로드 | ✅ 완료 |

#### 다음 시작점
1. 실기기에서 라이트/다크 테마 전환 테스트
2. Play Store 내부 테스트 테스터 추가 → 출시 검토 제출

---

### 14차 세션 (2026-03-31) — 앱 아이콘 디자인 및 적용

#### 아이콘 제작
| 항목 | 상태 |
|------|------|
| 아이콘 시안 5종 제작 (HTML SVG) | ✅ 완료 |
| B/C/D 컨셉 기반 개선 시안 제작 | ✅ 완료 |
| 최종 디자인 확정 (C개선판 — 이중링+불꽃) | ✅ 완료 |
| 1024×1024 PNG 내보내기 | ✅ 완료 |
| flutter_launcher_icons 적용 | ✅ 완료 |
| Android/iOS 전체 해상도 아이콘 생성 | ✅ 완료 |

#### 다음 시작점
새 아이콘이 적용된 릴리즈 빌드(v1.0.0+3) 생성 후 Play Store 비공개 테스트 등록

---

### 13차 세션 (2026-03-27) — 전수 버그 수정 + 수익성 개선 + 릴리즈 빌드

#### 버그 수정
| 항목 | 파일 |
|------|------|
| Firestore 인덱스 배포 완료 | firestore.indexes.json |
| 캘린더 빠른메뉴 2×2 그리드 카드로 개선 | profile_screen.dart |
| 통계 표시 2×2 그리드 → 단일 행 카드 | profile_screen.dart |
| 캘린더 월 이동 시 데이터 미갱신 수정 | focus_calendar_screen.dart |
| 집중 세션 캘린더 미기록 수정 (Timestamp 저장) | focus_session.dart |
| Firestore Timestamp/String 타입 불일치 수정 | focus_service.dart, user_model.dart |
| 크레딧 계산 targetMinutes → actualMinutes | focus_provider.dart |
| Timer 경쟁 조건 (_isCompleting 플래그) | focus_provider.dart |
| first_focus 배지 중복 지급 수정 | xp_service.dart |
| 스트릭 배지 타이밍 수정 (effectiveStreak +1) | xp_service.dart |
| 기프티콘 base64 크래시 수정 (_safeBase64Image) | my_gifticons_screen.dart |
| 랭킹 에러 메시지 사용자 친화적으로 교체 | ranking_screen.dart |
| Firestore 보안 규칙 강화 (익명만 write) | firestore.rules |
| user_notes 컬렉션 보안 규칙 추가 | firestore.rules |
| xpNeeded 선언 순서 오류 수정 | profile_screen.dart |
| my_gifticons_screen 괄호 오류 수정 | my_gifticons_screen.dart |

#### 수익성 개선
| 항목 | 파일 |
|------|------|
| 종료 광고 flat +15 → 기본크레딧 × 20% multiplier | constants.dart, credit_service.dart, focus_provider.dart |
| 상품 가격 인상 (편의점 1000→1500 등) | store_screen.dart, constants.dart |
| 버전 코드 1.0.0+1 → 1.0.0+2 | pubspec.yaml |
| 릴리즈 빌드 완료 (app-release.aab 51.9MB) | — |

#### 다음 시작점
1. 실기기 테스트 — 캘린더/종료광고/XP 화면 확인
2. Play Console aab 업로드 (v1.0.0+2) → 비공개 테스트 등록

---

### 12차 세션 (2026-03-25) — 11차 코드 전수 검토 및 버그 보완

#### 버그 수정
| 항목 | 파일 |
|------|------|
| 완료 화면 XP/배지 표시 타이밍 버그 — `_awardXp` await 완료 후 `completed` 상태 전환 | focus_provider.dart |
| Firestore 복합 인덱스 추가 — userId+completed+startedAt (캘린더 쿼리 오류 방지) | firestore.indexes.json |
| 알림 날짜 계산 개선 — `now.day+1` → `now.add(Duration(days:1))` 월말 경계 처리 | notification_service.dart |
| 에러 로깅 추가 — `catch (_) {}` → `debugPrint` | note_service.dart |

#### 다음 시작점
1. Firestore 인덱스 배포: `node "C:/Users/song/AppData/Roaming/npm/node_modules/firebase-tools/lib/bin/firebase.js" deploy --only firestore:indexes`
2. 실기기 테스트 — 완료 화면 XP/배지 동시 표시 확인 (12차 버그 수정 검증)
3. Play Store 릴리즈 빌드 재빌드 후 aab 업로드

---

### 11차 세션 (2026-03-25) — 게임화 기능 4가지 + 스킬 등록

#### 신규 기능
| 항목 | 파일 |
|------|------|
| 출석 체크 + XP 시스템 (앱 진입 시 자동 +10 XP, 집중 완료 시 분당 1 XP) | xp_service.dart, home_screen.dart |
| 레벨 시스템 (50레벨, 5단계 프레임, 칭호) | constants.dart, level_frame.dart |
| 업적/배지 12개 + 그리드 UI | achievements_screen.dart, xp_service.dart |
| 집중 캘린더 (월별 히트맵 + 세션 목록 + 메모 저장) | focus_calendar_screen.dart, note_service.dart |
| 알림 개선 — 집중 완료 시 오늘 알림 취소 후 내일로 재스케줄 | notification_service.dart |
| 완료 화면 — XP 획득량, 레벨업 배너, 배지 획득 카드 | focus_screen.dart |
| `/focus-cash`, `/focus-cash-save` Claude Code 스킬 등록 | ~/.claude/skills/ |

---

### 10차 세션 (2026-03-21) — 디자인 리뉴얼 계속 (랭킹/프로필/집중설정)

#### 디자인 리뉴얼 (진행 중)
**테마: 인디고(#4F46E5) + 시안(#06B6D4) + 골드(#F59E0B)**

| 항목 | 상태 | 파일 |
|------|------|------|
| 랭킹 화면 리뉴얼 | ✅ 완료 | ranking_screen.dart |
| 프로필 화면 리뉴얼 | ✅ 완료 | profile_screen.dart |
| 집중 설정 화면 리뉴얼 | ✅ 완료 | focus_setup_screen.dart |
| **로그인/온보딩 화면 리뉴얼** | ⬜ 미완 | login_screen.dart, onboarding ← **다음 시작점** |

##### 랭킹 화면 변경 내역
- 포디움(시상대) UI: 2위-1위-3위 순서, 각 랭크 컬러(골드/실버/브론즈) 글로우
- 메달 이모지 뱃지(🥇🥈🥉) 아바타 위 표시
- 4위 이하 리스트 카드 (내 순위 인디고 글로우 강조)
- 기간 선택 칩 primaryGradient 적용
- AppBar ShaderMask 그라디언트 타이틀
- 친구 없음 빈 상태 glowButton 추가

##### 프로필 화면 변경 내역
- 아바타에 primaryGradient 원형 테두리 + 인디고 글로우
- 전화번호 pill 배지로 표시
- 통계 카드 — 아이콘 배경 컨테이너(색상별) 추가
- 설정 목록을 단일 카드 컨테이너로 통합, 항목 간 구분선
- 로그아웃 OutlinedButton (빨간 테두리 스타일)
- 아바타 선택 그리드 — 선택 시 그라디언트 배경 + 글로우
- AppBar ShaderMask 그라디언트 타이틀

##### 집중 설정 화면 변경 내역
- 섹션 헤더에 컬러 세로 바 + 이모지 (⏱📚⚡)
- 프리셋/태그 칩을 커스텀 컨테이너로 교체 (선택 시 그라디언트+글로우)
- 타이머 숫자 박스에 borderMid 테두리 추가
- 모드 카드 — 아이콘 컨테이너 배경 색상 추가, 선택 시 글로우 효과
- 예상 크레딧 박스 아이콘 헤더 추가
- 시작 버튼 glowButton 데코레이션 적용
- AppBar ShaderMask 그라디언트 타이틀

#### Play Store 등록
| 항목 | 상태 |
|------|------|
| 릴리즈 빌드 생성 (app-release.aab) | ✅ 완료 |
| 키스토어 생성 | ✅ 완료 (android/app/focus_cash.jks, alias: mykey, pw: 046170) |
| build.gradle.kts 서명 설정 | ✅ 완료 |
| 개인정보처리방침 GitHub Pages 배포 | ✅ 완료 (https://songeunyeol.github.io/focus-cash-privacy/) |
| 비공개 테스트 aab 업로드 | ⬜ 미완 — 앱 콘텐츠 설정 먼저 필요 |

#### 다음 시작점
**A. 디자인 리뉴얼 마무리** — 로그인/온보딩 화면 리뉴얼 후 디자인 완성
**B. Play Store 등록 계속** — Play Console > 앱 콘텐츠 설정 완료 후 비공개 테스트 aab 업로드

---

### 9차 세션 (2026-03-21) — 디자인 리뉴얼 시작 + Play Store 등록 준비

#### 디자인 리뉴얼 (진행 중)
**테마: 인디고(#4F46E5) + 시안(#06B6D4) + 골드(#F59E0B)**

| 항목 | 상태 | 파일 |
|------|------|------|
| theme.dart 전체 교체 (인디고+시안 팔레트) | ✅ 완료 | lib/config/theme.dart |
| 하단 네비게이션 바 리뉴얼 | ✅ 완료 | home_screen.dart |
| 오늘의 현황 카드 리뉴얼 | ✅ 완료 | widgets/today_stats_card.dart |
| 집중 시작 버튼 리뉴얼 | ✅ 완료 | widgets/quick_start_button.dart |
| 상점 배경 + 탭바 게임샵 스타일 | ✅ 완료 | store_screen.dart |
| 교환 아이템 카드 게임샵 스타일 | ✅ 완료 | store_screen.dart |
| 오버스크롤 글로우 제거 (전역) | ✅ 완료 | main.dart |
| 최대 집중 시간 2시간 버그 수정 | ✅ 완료 | focus_setup_screen.dart |

#### Play Store 등록
| 항목 | 상태 |
|------|------|
| 릴리즈 빌드 생성 (app-release.aab) | ✅ 완료 |
| 키스토어 생성 | ✅ 완료 |
| 개인정보처리방침 GitHub Pages 배포 | ✅ 완료 |

---

### 8차 세션 (2026-03-20) — 버그 수정 + 실기기 테스트 완료 + Play Store 등록 시작

#### 버그 수정
| 항목 | 파일 |
|------|------|
| 배송지 입력 시 `_dependents.isEmpty` crash — 모달 애니메이션 중 TextEditingController dispose 문제 → _DeliveryFormSheet 별도 StatefulWidget으로 분리 | my_gifticons_screen.dart |

#### 완료
| 항목 | 비고 |
|------|------|
| AdMob 실제 ID 교체 완료 | AndroidManifest.xml, ad_service.dart |
| Firebase 익명 로그인 활성화 확인 | 관리자 앱용 |
| 실기기 전체 플로우 테스트 완료 | 로그인/집중/교환/룰렛/응모/친구/크레딧 내역 모두 확인 |
| Google Play Console 앱 생성 완료 | 앱 이름: Focus Cash |

#### 다음 시작점 — Play Store 등록 3단계
```
C:\src\flutter\bin\flutter.bat build appbundle --release
```
결과물: `build/app/outputs/bundle/release/app-release.aab`

이후 순서:
4. aab 파일 Play Console에 업로드
5. 스토어 등록 정보 작성 (설명, 스크린샷, 아이콘)
6. 콘텐츠 등급 설문 작성
7. 검토 제출

---

### 7차 세션 (2026-03-18) — 품질 개선 + 데모 코드 제거

#### 버그 수정
| 항목 | 파일 |
|------|------|
| credit_history_screen — Firestore 미연동 (빈 화면) → CreditService 실 연동 | credit_history_screen.dart |
| credit_service.addCredits() — todayCredits 미업데이트 → 날짜 리셋 포함 업데이트 | credit_service.dart |
| my_gifticons_screen — context.read → context.watch (실시간 갱신) | my_gifticons_screen.dart |
| focus_setup_screen — 하드코어 예상 크레딧 1.2배 미반영 → 반영 | focus_setup_screen.dart |
| firestore.rules gifticon_codes — 필드명 오류(userId→usedBy) + count() 권한 | firestore.rules |
| 룰렛 슬롯머신 아이템 — '가중치 N' → 확률 % 표시 | store_screen.dart |
| 관리자 앱 Firebase 연결 끊김 — store_items/raffle_rooms/roulette_config 쓰기 규칙 없음 | firestore.rules |
| home_screen didChangeAppLifecycleState — mounted 체크 없이 context 사용 | home_screen.dart |
| focus_screen didChangeAppLifecycleState — mounted 체크 없이 context 사용 | focus_screen.dart |
| focus_screen dispose() — context.read로 FocusProvider 접근 → ref 사전 저장 방식 | focus_screen.dart |

#### 코드 정리
| 항목 | 비고 |
|------|------|
| 데모 유저 코드 전체 제거 | setDemoUser(), isDemoMode, 데모 데이터 생성 함수 |
| 카카오 앱 키 secrets.dart로 이동 | main.dart에서 AppSecrets.kakaoNativeAppKey 참조 |
| friend_requests 복합 인덱스 4개 추가 | Firebase 배포 완료 |

### 6차 세션 (2026-03-17) — 룰렛 보완 + 이전 세션 버그 수정

#### 기능 추가 (룰렛)
| 항목 | 비고 |
|------|------|
| 룰렛 상품에 이미지 추가 | 어드민앱에서 이미지 업로드 |
| 룰렛 당첨 상품 → 내 기프티콘 표시 | _RoulettePrizeCard |
| 룰렛 확률 → 가중치 자동계산 방식 | totalWeight 기반 |
| 룰렛 기프티콘 재고 관리 | 교환창과 동일한 GifticonCodesScreen 재사용 |
| 품절 상품 룰렛 제외 | 스핀 직전 Firestore 재조회 |

### 5차 세션 (2026-03-16) — 직접배송 기능
| 항목 |
|------|
| 직접배송 당첨 → 배송지 입력 UI (_DirectDeliveryCard) |
| submitDeliveryInfo() + 텔레그램 알림 발송 |

### 4차 세션 (2026-03-16) — 상점 고도화 + 전체 버그 수정
| 항목 |
|------|
| 응모방 크레딧 풀 방식 완전 재설계 |
| 랭킹 화면 Firestore 연동 (일간/주간/월간) |
| 텔레그램 봇 당첨 알림 |
| 텔레그램 토큰 secrets.dart로 분리 |

### 3차 세션 (2026-03-12) — 룰렛 슬롯머신 UI
| 항목 |
|------|
| 슬롯머신 방식 전환 (ListWheelScrollView) |
| 2단계 스핀 애니메이션, 햅틱 피드백 |

### 2차 세션 (2026-03-11) — 로그인·홈·타이머
| 항목 |
|------|
| 구글/카카오 소셜 로그인 |
| 홈 스트릭·주간차트 |
| 타이머 팝업/일시정지 제거 |
| 하드코어 1.2배 보너스 |

### 1차 세션 — 초기 구축
| 항목 |
|------|
| Flutter 프로젝트 생성 (앱+관리자) |
| Firebase 연동, Firestore 생성 |
| 집중 타이머 핵심 로직 |
| 관리자 앱 PIN·CRUD |
| AdMob 연동 (테스트 ID) |

---

## 출시 전 필수 체크리스트

- [ ] **서버 이관 (docs/SERVER_MIGRATION.md 0~4단계)** ← 19차 신규, **출시 차단 사항**
  - [ ] 배포 규칙 백업 → 시크릿 회전 → Blaze → Functions 배포 → set-admin
  - [ ] `AppConfig.useServerSettlement = true` 후 내부 테스트 완주 확인
  - [ ] `firestore.rules.next` 배포 + 익명 로그인 비활성화
- [x] **AdMob 실제 ID 교체** ✅
- [x] ~~Firebase Console — 익명 로그인 활성화 확인~~ → 19차: 관리자 앱이 Google+claim 으로 바뀌어 **비활성화 대상**
- [x] **실기기 전체 플로우 테스트** ✅ (8차 기본 플로우 완료, 11차 게임화 기능 테스트 필요)
- [ ] **Google Play Store 등록** ← 진행 중
  - [x] 1. Play Console 접속
  - [x] 2. 앱 만들기 (Focus Cash)
  - [x] 3. 릴리즈 빌드 생성 완료 (키스토어: android/app/focus_cash.jks, alias: mykey, pw: 046170)
  - [x] 4. aab 파일 업로드 ✅ (v1.0.0+3 내부 테스트 업로드 완료)
  - [ ] 5. 테스터 추가 + 앱 콘텐츠 설문 완료 ← **다음 시작점**
  - [ ] 6. 비공개 테스트 12명 × 14일 모집
  - [ ] 7. 프로덕션 신청
